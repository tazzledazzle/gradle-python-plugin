---
phase: 02-v1.1-features
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt
  - src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecScriptTest.kt
  - src/functionalTest/kotlin/com/tazzledazzle/python/PythonExecScriptFunctionalTest.kt
  - docs/DSL.md
  - docs/MIGRATION.md
autonomous: true
requirements:
  - EXEC-06
  - EXEC-07
user_setup: []

must_haves:
  truths:
    - "PythonExec runs .py files via managed python when script is set"
    - "script and venvExec together fail at buildCommand with clear GradleException"
    - "script and executable together fail at buildCommand with clear GradleException"
    - "TestKit validates script mode end-to-end without network"
  artifacts:
    - path: src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt
      provides: "script InputFile property and command assembly"
      contains: "script"
    - path: src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecScriptTest.kt
      provides: "Unit tests for script mode and validation"
      contains: "mutually exclusive"
    - path: src/functionalTest/kotlin/com/tazzledazzle/python/PythonExecScriptFunctionalTest.kt
      provides: "Offline functional test for script mode"
      contains: "script"
    - path: docs/DSL.md
      provides: "Shipped v1.1 API without deferral section"
      contains: "script"
  key_links:
    - from: PythonExec.buildCommand()
      to: PythonEnvService.resolveExecutable("python")
      via: "script mode command prefix"
      pattern: "resolveExecutable\\(\"python\"\\)"
    - from: PythonExecScriptFunctionalTest.kt
      to: prepareCondaSentinel + fake python
      via: "TestKit build script"
      pattern: "prepareCondaSentinel"
---

<objective>
Add `script` execution mode to `PythonExec`: run `.py` files via managed `python`, with mutual-exclusivity validation against `venvExec` and `executable` (EXEC-06, EXEC-07).

Purpose: Replaces manual `executable` + script-path-in-arguments pattern with env-aware DSL; completes v1.1 public API.
Output: Extended `PythonExec.kt`, unit + functional tests, updated DSL/MIGRATION docs with deferrals removed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-v1.1-features/02-CONTEXT.md
@.planning/phases/02-v1.1-features/02-RESEARCH.md
@src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt
@src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecVenvExecTest.kt
@src/functionalTest/kotlin/com/tazzledazzle/python/ParallelExecutionFunctionalTest.kt
@docs/DSL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement script property and buildCommand validation (EXEC-06, EXEC-07)</name>
  <files>src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt</files>
  <read_first>src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt, src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecVenvExecTest.kt, .planning/phases/02-v1.1-features/02-CONTEXT.md</read_first>
  <behavior>
    - script set: buildCommand returns [managedPythonPath, scriptAbsolutePath] + arguments
    - script + venvExec: GradleException before resolution with message containing "script" and "venvExec"
    - script + executable: GradleException before resolution with message containing "script" and "executable"
    - script unset: existing venvExec/executable behavior unchanged
  </behavior>
  <action>
    Extend `PythonExec.kt` per script execution locked decision:

    1. Add imports: `org.gradle.api.tasks.InputFile`, `RegularFileProperty` (if not already).
    2. Declare `@get:InputFile` `@get:Optional` `abstract val script: RegularFileProperty`.
    3. At start of `buildCommand()` (before any resolveExecutable calls), validate:
       - If `script.isPresent && venvExec.isPresent`: throw `GradleException` with message "PythonExec 'script' cannot be used together with 'venvExec'." (EXEC-07)
       - If `script.isPresent && executable.isPresent`: throw `GradleException` with message "PythonExec 'script' cannot be used together with 'executable'." (per CONTEXT: executable for non-script tools only)
    4. Refactor `resolveExecutableCommand()` to handle script mode:
       - When `script.isPresent`: return `envService.get().resolveExecutable("python").absolutePath`
       - Preserve existing venvExec and executable branches when script absent
       - Update else branch error to: "PythonExec requires 'script', 'venvExec', or 'executable'."
    5. In `buildCommand()`: when script present, return `listOf(exe, script.get().asFile.absolutePath) + arguments.get()`; else `listOf(exe) + arguments.get()`.

    Do not change `executeProcess()` capture/exit logic except what already exists from 02-02 outputFile.
  </action>
  <acceptance_criteria>
    - `PythonExec.kt` contains `@get:InputFile` and `abstract val script: RegularFileProperty`
    - `buildCommand()` validates script+venvExec and script+executable before calling resolveExecutable
    - Script mode command includes script file absolute path as second element
    - `./gradlew compileKotlin --no-daemon` exits 0
  </acceptance_criteria>
  <verify>
    <automated>./gradlew compileKotlin --no-daemon</automated>
  </verify>
  <done>EXEC-06/07: script property and mutual-exclusivity validation implemented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for script mode and validation</name>
  <files>src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecScriptTest.kt</files>
  <read_first>src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecVenvExecTest.kt, src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecTest.kt, src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt</read_first>
  <behavior>
    - buildCommand with script only: first element is fake python path, second is script absolute path
    - script + venvExec: assertFailsWith GradleException, message mentions script and venvExec
    - script + executable: assertFailsWith GradleException, message mentions script and executable
    - script with extra arguments: arguments appended after script path
  </behavior>
  <action>
    Create `PythonExecScriptTest.kt` following `PythonExecVenvExecTest` envService mock pattern:

    1. Set up temp project with uv envManagerType, fake `.venv/bin/python` executable (shell script echoing ok), register `pythonEnvServiceTest` BuildService, wire `task.envService`.
    2. Create temp `.py` file under project dir.
    3. Test `buildCommand resolves script via managed python` — set script to .py file, assert command equals `[python.canonicalPath, script.canonicalPath]` (+ args if set).
    4. Test `buildCommand rejects script with venvExec` — set both, assertFailsWith message containing "script" and "venvExec".
    5. Test `buildCommand rejects script with executable` — set both, assertFailsWith message containing "script" and "executable".

    Use `buildCommand()` only for command assembly tests; optional executeProcess test with fake python stub if straightforward.
  </action>
  <acceptance_criteria>
    - `PythonExecScriptTest.kt` has at least 3 tests covering script command shape and both mutual-exclusivity cases
    - `./gradlew test --tests "com.tazzledazzle.python.tasks.PythonExecScriptTest" --no-daemon` exits 0
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.tazzledazzle.python.tasks.PythonExecScriptTest" --no-daemon</automated>
  </verify>
  <done>EXEC-06/07 unit coverage: script command assembly and validation errors verified</done>
</task>

<task type="auto">
  <name>Task 3: Script functional test and ship v1.1 DSL docs</name>
  <files>src/functionalTest/kotlin/com/tazzledazzle/python/PythonExecScriptFunctionalTest.kt, docs/DSL.md, docs/MIGRATION.md</files>
  <read_first>src/functionalTest/kotlin/com/tazzledazzle/python/ParallelExecutionFunctionalTest.kt, src/functionalTest/kotlin/com/tazzledazzle/python/PythonExecFunctionalTest.kt, docs/DSL.md, docs/MIGRATION.md</read_first>
  <action>
    Create `PythonExecScriptFunctionalTest.kt` per Testing locked decision (sentinel pattern, no network):

    1. Temp project with plugin applied.
    2. Register `prepareCondaSentinel` writing `.gradle/python/conda/24.11.3-0/.installed` (copy from ParallelExecutionFunctionalTest).
    3. Register task creating fake managed `python` at uv-style path `.venv/bin/python` (shell: `#!/bin/sh` + `printf 'script-ok'`) — mirror RESEARCH pitfall 4.
    4. Write `scripts/hello.py` placeholder file (content irrelevant if python stub ignores it).
    5. Register `PythonExec` task `runScript` with `script.set(layout.projectDirectory.file("scripts/hello.py"))`, `dependsOn("prepareCondaSentinel")`, optional `outputFile` to assert file write.
    6. GradleRunner `runScript` → SUCCESS; optionally assert output file contains `script-ok`.

    Update `docs/DSL.md`:
    - Add `script` row to PythonExec property table (@InputFile, optional, mutually exclusive with venvExec/executable)
    - Add script mode Kotlin example using `script.set(...)` and `arguments`
    - Remove entire "## Deferred to v1.1" section (outputFile and script now shipped)
    - Update document title/version reference from v1.0 to v1.1

    Update `docs/MIGRATION.md`: change v1.1 script row from "coming soon" to concrete `script.set(...)` example.
  </action>
  <acceptance_criteria>
    - `PythonExecScriptFunctionalTest.kt` exists and uses `prepareCondaSentinel`
    - `./gradlew functionalTest --tests "com.tazzledazzle.python.PythonExecScriptFunctionalTest" --no-daemon` exits 0
    - `docs/DSL.md` does NOT contain "Deferred to v1.1"
    - `docs/DSL.md` contains `script` property in PythonExec table
  </acceptance_criteria>
  <verify>
    <automated>./gradlew functionalTest --tests "com.tazzledazzle.python.PythonExecScriptFunctionalTest" --no-daemon && ! grep -q 'Deferred to v1.1' docs/DSL.md</automated>
  </verify>
  <done>Script mode validated end-to-end; v1.1 API fully documented without deferrals</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script InputFile → process execution | User-supplied .py path executed by managed python |
| fake python stub → TestKit | Test-only executable in temp project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | script path points outside project | mitigate | Gradle InputFile resolves relative to project; document convention in DSL |
| T-02-06 | Elevation | Arbitrary code via .py script | transfer | User owns script content; same trust as venvExec tools |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs |
</threat_model>

<verification>
`./gradlew check --no-daemon` passes all unit and functional tests including script mode.
</verification>

<success_criteria>
- EXEC-06: script mode runs .py via `envService.resolveExecutable("python")`
- EXEC-07: script ⊥ venvExec fails with clear error; script ⊥ executable also rejected
- Functional test passes offline; DSL and MIGRATION reflect shipped v1.1 API
</success_criteria>

<output>
Create `.planning/phases/02-v1.1-features/02-03-SUMMARY.md` when done
</output>
