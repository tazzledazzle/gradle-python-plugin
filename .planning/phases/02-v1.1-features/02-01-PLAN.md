---
phase: 02-v1.1-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/functionalTest/kotlin/com/tazzledazzle/python/PythonExecFunctionalTest.kt
  - docs/MIGRATION.md
  - docs/DSL.md
  - README.md
autonomous: true
requirements:
  - TEST-01
  - DOC-01
  - DOC-02
user_setup: []

must_haves:
  truths:
    - "Gradle TestKit runs PythonExec end-to-end with explicit executable and no network download"
    - "docs/MIGRATION.md maps v1 workaround patterns to plugin-native replacements"
    - "docs/DSL.md documents ignoreExitValue warn-only and conditional logging patterns"
  artifacts:
    - path: src/functionalTest/kotlin/com/tazzledazzle/python/PythonExecFunctionalTest.kt
      provides: "TEST-01 functional coverage for PythonExec baseline"
      contains: "GradleRunner"
    - path: docs/MIGRATION.md
      provides: "Pre-v1 workaround migration guide"
      contains: "ignoreExitValue"
    - path: docs/DSL.md
      provides: "ignoreExitValue policy examples"
      contains: "ignoreExitValue"
  key_links:
    - from: PythonExecFunctionalTest.kt
      to: com.tazzledazzle.python plugin
      via: "GradleRunner.withPluginClasspath()"
      pattern: "withPluginClasspath"
    - from: docs/MIGRATION.md
      to: docs/DSL.md
      via: "cross-link"
      pattern: "DSL\\.md"
---

<objective>
Establish release hardening baseline before v1.1 execution features land: offline TestKit coverage for `PythonExec`, a migration guide for pre-v1 workarounds, and expanded DSL examples for `ignoreExitValue`.

Purpose: Validates existing execution path and documents upgrade path so v1.1 API changes ship with test and doc guardrails (TEST-01, DOC-01, DOC-02).
Output: `PythonExecFunctionalTest.kt`, `docs/MIGRATION.md`, updated `docs/DSL.md` and README link.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-v1.1-features/02-CONTEXT.md
@.planning/phases/02-v1.1-features/02-RESEARCH.md
@src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt
@src/functionalTest/kotlin/com/tazzledazzle/python/ParallelExecutionFunctionalTest.kt
@src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecTest.kt
@docs/DSL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PythonExec TestKit functional test (TEST-01)</name>
  <files>src/functionalTest/kotlin/com/tazzledazzle/python/PythonExecFunctionalTest.kt</files>
  <read_first>src/functionalTest/kotlin/com/tazzledazzle/python/ParallelExecutionFunctionalTest.kt, src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecTest.kt, src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt, build.gradle.kts</read_first>
  <action>
    Create `PythonExecFunctionalTest.kt` in package `com.tazzledazzle.python` per Testing locked decision (no network in default functional tests).

    Test method: `pythonExec with explicit executable succeeds and captures output` — use temp directory via `Files.createTempDirectory("python-exec-functional")`, write minimal `settings.gradle.kts` (rootProject.name only) and `build.gradle.kts` that applies `id("com.tazzledazzle.python")` and registers a `PythonExec` task named `runEcho`.

    Use platform branching matching `PythonExecTest.successCommand()` pattern: on Windows set `executable` to `cmd` with arguments `listOf("/c", "echo hello")`; on Unix set `executable` to `sh` with arguments `listOf("-c", "printf hello")`. Do NOT use `venvExec`, `script`, or conda sentinel — this test validates explicit-executable mode only.

    Run with `GradleRunner.create().withProjectDir(projectDir).withPluginClasspath().withArguments("runEcho").build()`.

    Assert `TaskOutcome.SUCCESS` for `:runEcho`. Do not add network-dependent env bootstrap.
  </action>
  <acceptance_criteria>
    - File `src/functionalTest/kotlin/com/tazzledazzle/python/PythonExecFunctionalTest.kt` exists and contains class `PythonExecFunctionalTest`
    - Test uses `GradleRunner` with `withPluginClasspath()` and task name `runEcho`
    - `./gradlew functionalTest --tests "com.tazzledazzle.python.PythonExecFunctionalTest" --no-daemon` exits 0
  </acceptance_criteria>
  <verify>
    <automated>./gradlew functionalTest --tests "com.tazzledazzle.python.PythonExecFunctionalTest" --no-daemon</automated>
  </verify>
  <done>TEST-01 passes offline: plugin applies in temp project, PythonExec task completes SUCCESS with explicit sh/cmd executable</done>
</task>

<task type="auto">
  <name>Task 2: Create migration guide (DOC-01)</name>
  <files>docs/MIGRATION.md, README.md</files>
  <read_first>docs/DSL.md, README.md, .planning/phases/02-v1.1-features/02-CONTEXT.md, src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt</read_first>
  <action>
    Create `docs/MIGRATION.md` per Documentation locked decision. Include sections:

    1. **Overview** — upgrading from pre-v1 shell/`doLast` workarounds to `com.tazzledazzle.python` v1.0+ APIs.
    2. **Workaround replacement table** with at least these rows:
       - Manual `doLast { if (exitValue != 0) throw ... }` → `ignoreExitValue.set(false)` (default) or `ignoreExitValue.set(true)` for warn-only
       - Hard-coded conda/venv paths in `executable` → `venvExec.set("toolname")` with `envSetup` / `PythonEnvService`
       - `python script.py` via `executable` + path in `arguments` → note v1.1 `script` property (link to DSL; mark as v1.1 when not yet shipped)
       - Legacy `useVenv` toggle → removed; use `venvExec`
    3. **Before/after Gradle snippets** for at least exit-code policy and managed-env tool invocation.
    4. Link to `docs/DSL.md` as source of truth.

    Add a link to `docs/MIGRATION.md` from `README.md` in an appropriate docs section (e.g., near DSL or usage heading). Do not remove existing README content.
  </action>
  <acceptance_criteria>
    - `docs/MIGRATION.md` exists and contains heading `# Migration Guide`
    - File contains strings `ignoreExitValue`, `venvExec`, and `doLast`
    - `README.md` contains reference to `docs/MIGRATION.md` or `MIGRATION.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/MIGRATION.md && grep -q 'ignoreExitValue' docs/MIGRATION.md && grep -q 'MIGRATION' README.md</automated>
  </verify>
  <done>DOC-01 complete: migration guide published and linked from README</done>
</task>

<task type="auto">
  <name>Task 3: Expand DSL ignoreExitValue examples (DOC-02)</name>
  <files>docs/DSL.md</files>
  <read_first>docs/DSL.md, src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt, docs/MIGRATION.md</read_first>
  <action>
    Expand `docs/DSL.md` PythonExec section per Documentation locked decision (DOC-02). Add subsections after existing examples:

    1. **Warn-only quality gate** — `venvExec` + `ignoreExitValue.set(true)` for lint/audit tasks that should not fail CI; show reading `exitValue` in `doLast` for conditional logging.
    2. **Strict vs warn-only comparison** — side-by-side snippets: default fail-fast vs warn-only with post-task log when `exitValue.get() != 0`.
    3. **Cross-reference** — link to `docs/MIGRATION.md` for users migrating from manual exit checks.

    Keep existing v1.0 property table intact. Do NOT remove "Deferred to v1.1" section yet (removed in plan 02-03 when features ship). Do NOT document `outputFile` or `script` here yet.
  </action>
  <acceptance_criteria>
    - `docs/DSL.md` contains at least two distinct `ignoreExitValue.set(true)` Kotlin examples
    - `docs/DSL.md` references `exitValue.get()` in a conditional logging example
    - `docs/DSL.md` links to `MIGRATION.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'ignoreExitValue.set(true)' docs/DSL.md | awk '{exit ($1 >= 2) ? 0 : 1}'</automated>
  </verify>
  <done>DOC-02 complete: DSL documents ignoreExitValue policy patterns with concrete examples</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TestKit temp project → host FS | Functional test writes ephemeral build scripts; no user data |
| Docs → user build scripts | Migration examples are copy-paste guidance only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | TestKit build.gradle.kts strings | accept | Ephemeral temp dirs; no production impact |
| T-02-02 | Information Disclosure | MIGRATION.md examples | accept | No secrets; uses placeholder paths only |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Run `./gradlew check --no-daemon` — all existing tests plus new functional test pass. Confirm docs exist and cross-link.
</verification>

<success_criteria>
- TEST-01: `PythonExecFunctionalTest` passes in `functionalTest` source set
- DOC-01: `docs/MIGRATION.md` documents workaround replacements
- DOC-02: `docs/DSL.md` has expanded `ignoreExitValue` examples
</success_criteria>

<output>
Create `.planning/phases/02-v1.1-features/02-01-SUMMARY.md` when done
</output>
