---
phase: 02-v1.1-features
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - src/functionalTest/kotlin/com/tazzledazzle/python/BackendParityFunctionalTest.kt
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TEST-02
user_setup: []

must_haves:
  truths:
    - "Backend parity test runs only when CI_NETWORK_TESTS=true"
    - "Default PR CI does not download Miniforge/uv (existing functional tests unchanged)"
    - "Main-branch push triggers optional integration job with network tests"
  artifacts:
    - path: src/functionalTest/kotlin/com/tazzledazzle/python/BackendParityFunctionalTest.kt
      provides: "Gated network envSetup validation"
      contains: "CI_NETWORK_TESTS"
    - path: .github/workflows/ci.yml
      provides: "Separate integration job on main push"
      contains: "CI_NETWORK_TESTS"
  key_links:
    - from: BackendParityFunctionalTest.kt
      to: EnvSetupTask
      via: "real envSetup without sentinel"
      pattern: "envSetup"
    - from: ci.yml integration job
      to: BackendParityFunctionalTest
      via: "functionalTest --tests filter"
      pattern: "BackendParityFunctionalTest"
---

<objective>
Add optional network-backed backend parity validation (TEST-02): real `envSetup` without sentinel stub, gated to main-branch CI only.

Purpose: Validates conda bootstrap against live download on main without slowing or flaking PR checks (Testing locked decision: CI_NETWORK_TESTS gate).
Output: `BackendParityFunctionalTest.kt`, optional `integration` job in `ci.yml`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-v1.1-features/02-CONTEXT.md
@.planning/phases/02-v1.1-features/02-RESEARCH.md
@src/functionalTest/kotlin/com/tazzledazzle/python/ParallelExecutionFunctionalTest.kt
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gated BackendParityFunctionalTest (TEST-02)</name>
  <files>src/functionalTest/kotlin/com/tazzledazzle/python/BackendParityFunctionalTest.kt</files>
  <read_first>src/functionalTest/kotlin/com/tazzledazzle/python/ParallelExecutionFunctionalTest.kt, .planning/phases/02-v1.1-features/02-RESEARCH.md, build.gradle.kts</read_first>
  <action>
    Create `BackendParityFunctionalTest.kt` per Testing locked decision (TEST-02 optional, network gated):

    1. Annotate test class or method with JUnit 5 `@EnabledIfEnvironmentVariable(named = "CI_NETWORK_TESTS", matches = "true")`. Add `import org.junit.jupiter.api.condition.EnabledIfEnvironmentVariable` — verify JUnit Jupiter is on functionalTest classpath; if missing, add `functionalTestImplementation("org.junit.jupiter:junit-jupiter")` to `build.gradle.kts` only if compile fails.
    2. Test `conda envSetup creates install sentinel without stub`:
       - Temp project with plugin, NO `prepareCondaSentinel` task
       - Register default `envSetup` (EnvSetupTask) or invoke `envSetup` task
       - GradleRunner withArguments `envSetup`, timeout generous (e.g. 10 minutes via runner config if supported, or rely on CI job timeout)
       - Assert SUCCESS and `.gradle/python/conda/24.11.3-0/.installed` exists (match default condaVersion from extension)
    3. Do NOT run this test in default `./gradlew functionalTest` on developer machines without env var — gate ensures skip.

    Optional second test for uv backend behind same env gate — only if straightforward; one conda test satisfies TEST-02 minimum.
  </action>
  <acceptance_criteria>
    - `BackendParityFunctionalTest.kt` contains `@EnabledIfEnvironmentVariable(named = "CI_NETWORK_TESTS"`
    - `./gradlew functionalTest --tests "com.tazzledazzle.python.BackendParityFunctionalTest" --no-daemon` exits 0 with tests skipped (no env var)
    - `./gradlew functionalTest --tests "com.tazzledazzle.python.BackendParityFunctionalTest" --no-daemon` runs test when `CI_NETWORK_TESTS=true` is set (manual verification acceptable in SUMMARY if no network in planner environment)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew functionalTest --tests "com.tazzledazzle.python.BackendParityFunctionalTest" --no-daemon</automated>
  </verify>
  <done>TEST-02 test class exists and skips by default; runs only with CI_NETWORK_TESTS=true</done>
</task>

<task type="auto">
  <name>Task 2: Add main-only integration CI job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml, .planning/phases/02-v1.1-features/02-CONTEXT.md, src/functionalTest/kotlin/com/tazzledazzle/python/BackendParityFunctionalTest.kt</read_first>
  <action>
    Extend `.github/workflows/ci.yml` with separate job `integration` per RESEARCH Pattern 6:

    1. Job name: `integration`
    2. Condition: `if: github.event_name == 'push' && github.ref == 'refs/heads/main'` — NOT on pull_request
    3. runs-on: ubuntu-latest
    4. Same Java 21 + setup-gradle steps as `build-test`
    5. env: `CI_NETWORK_TESTS: true`
    6. Step: `./gradlew functionalTest --tests "*BackendParityFunctionalTest" --no-daemon`
    7. timeout-minutes: 15 (generous for Miniforge download)

    Do NOT add network tests to existing `build-test` job — PR CI must remain fast/offline.
    Do NOT use `continue-on-error` unless test proves flaky after implementation (Claude's discretion: start strict).
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains job id `integration`
    - Job condition restricts to push on main branch only
    - Job sets `CI_NETWORK_TESTS: true` and runs BackendParityFunctionalTest filter
    - Existing `build-test` job unchanged (still runs detekt, test, functionalTest without env var)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'integration:' .github/workflows/ci.yml && grep -q 'CI_NETWORK_TESTS' .github/workflows/ci.yml && grep -q 'BackendParityFunctionalTest' .github/workflows/ci.yml</automated>
  </verify>
  <done>TEST-02 wired: main push runs network parity job; PR CI unaffected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → Miniforge/uv CDN | Network download of bootstrap artifacts |
| TestKit → GitHub Actions | Ephemeral temp projects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | CDN download MITM | transfer | HTTPS to official release URLs; same as production envSetup |
| T-02-08 | Denial of Service | Network timeout fails main | mitigate | Separate job; 15m timeout; optional disable if flaky per discretion |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs beyond possible JUnit Jupiter if needed |
</threat_model>

<verification>
Default `./gradlew check` passes with BackendParity tests skipped. CI yaml valid. Document in SUMMARY that full network test validates on first main push.
</verification>

<success_criteria>
- TEST-02: Gated functional test and main-only CI job exist
- PR workflow unchanged for network behavior
- All non-gated tests still pass locally and in build-test job
</success_criteria>

<output>
Create `.planning/phases/02-v1.1-features/02-04-SUMMARY.md` when done
</output>
