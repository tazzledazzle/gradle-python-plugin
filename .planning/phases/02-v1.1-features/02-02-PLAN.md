---
phase: 02-v1.1-features
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt
  - src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecOutputFileTest.kt
  - docs/DSL.md
autonomous: true
requirements:
  - EXEC-05
user_setup: []

must_haves:
  truths:
    - "PythonExec writes captured stdout to outputFile after process completes"
    - "outputFile parent directories are created before write"
    - "outputFile is written even when process exits non-zero (before fail-fast throw)"
  artifacts:
    - path: src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt
      provides: "outputFile RegularFileProperty with @OutputFile"
      contains: "outputFile"
    - path: src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecOutputFileTest.kt
      provides: "Unit tests for stdout persistence"
      contains: "outputFile"
    - path: docs/DSL.md
      provides: "outputFile property documentation"
      contains: "outputFile"
  key_links:
    - from: PythonExec.kt executeProcess()
      to: outputFile RegularFileProperty
      via: "writeText after stdout.set(out)"
      pattern: "outputFile\\.orNull"
    - from: PythonExecOutputFileTest.kt
      to: PythonExec.executeProcess()
      via: "ProjectBuilder task execution"
      pattern: "executeProcess"
---

<objective>
Add `outputFile` to `PythonExec` so captured stdout persists to a Gradle-tracked output file after execution (EXEC-05).

Purpose: Enables downstream tasks to depend on file outputs without shell redirects; honors locked decision to write post-capture, not via shell redirect.
Output: Extended `PythonExec.kt`, `PythonExecOutputFileTest.kt`, updated `docs/DSL.md` property table and example.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-v1.1-features/02-CONTEXT.md
@.planning/phases/02-v1.1-features/02-RESEARCH.md
@src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt
@src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecTest.kt
@docs/DSL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement outputFile property and write logic (EXEC-05)</name>
  <files>src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt</files>
  <read_first>src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt, .planning/phases/02-v1.1-features/02-CONTEXT.md, .planning/phases/02-v1.1-features/02-RESEARCH.md</read_first>
  <behavior>
    - When outputFile is set and process succeeds: file content equals captured stdout
    - When outputFile path includes nested dirs: parent directories created, file written
    - When process fails with ignoreExitValue false: stdout still written to outputFile before GradleException
    - When outputFile unset: no file write; existing behavior unchanged
  </behavior>
  <action>
    Extend `PythonExec.kt` per outputFile locked decision (EXEC-05):

    1. Add imports: `org.gradle.api.file.RegularFileProperty`, `org.gradle.api.tasks.OutputFile`.
    2. Declare property:
       - `@get:OutputFile` `@get:Optional` `abstract val outputFile: RegularFileProperty`
    3. In `executeProcess()`, after `stdout.set(out)` and before exit-code check:
       - If `outputFile.orNull != null`: call `file.parentFile.mkdirs()` then `file.writeText(out)`
    4. Do NOT shell-redirect stdout in `buildCommand()` — write uses captured `out` string only.
    5. Write must occur regardless of exit code (locked decision / pitfall 5 in RESEARCH).

    Do not modify `buildCommand()` resolution logic in this task.
  </action>
  <acceptance_criteria>
    - `PythonExec.kt` contains `@get:OutputFile` and `abstract val outputFile: RegularFileProperty`
    - `executeProcess()` calls `writeText` on output file after `stdout.set(out)` and before exit check
    - `./gradlew compileKotlin --no-daemon` exits 0
  </acceptance_criteria>
  <verify>
    <automated>./gradlew compileKotlin --no-daemon</automated>
  </verify>
  <done>EXEC-05 implementation: outputFile property declared and stdout persisted post-capture</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests and DSL docs for outputFile</name>
  <files>src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecOutputFileTest.kt, docs/DSL.md</files>
  <read_first>src/test/kotlin/com/tazzledazzle/python/tasks/PythonExecTest.kt, src/main/kotlin/com/tazzledazzle/python/tasks/PythonExec.kt, docs/DSL.md</read_first>
  <behavior>
    - Success path: outputFile content equals stdout ("hello" from sh/cmd fixture)
    - Nested path: `build/reports/out.txt` created with correct content
    - Failure path with ignoreExitValue false: file still contains stdout before exception
    - No outputFile: no exception from missing file logic
  </behavior>
  <action>
    Create `PythonExecOutputFileTest.kt` using `ProjectBuilder` and `successCommand()` / `failureCommand()` / `isWindows()` patterns copied from `PythonExecTest.kt` (Claude's discretion).

    Tests required:
    1. `writes captured stdout to outputFile on success` — set nested output path under project dir, assert file text equals stdout.
    2. `writes stdout to outputFile before failing on non-zero exit` — use failureCommand(), do not set ignoreExitValue, assertFailsWith GradleException, assert file contains "fail".
    3. `creates parent directories for nested outputFile` — use path like `build/nested/deep/out.txt`.

    Do NOT test via shell redirect into output file — that bypasses plugin logic.

    Update `docs/DSL.md` PythonExec property table: add row for `outputFile` | `RegularFile` | optional | "Persists captured stdout after execution". Add Kotlin example showing `outputFile.set(layout.buildDirectory.file("reports/python-out.txt"))` with `venvExec` or `executable`. Keep "Deferred to v1.1" table entry for `outputFile` until plan 02-03 removes deferrals (or remove `outputFile` row from deferred table now since it ships in this plan).
  </action>
  <acceptance_criteria>
    - `PythonExecOutputFileTest.kt` contains at least 3 `@Test` methods referencing `outputFile`
    - `./gradlew test --tests "com.tazzledazzle.python.tasks.PythonExecOutputFileTest" --no-daemon` exits 0
    - `docs/DSL.md` property table contains `outputFile` row
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.tazzledazzle.python.tasks.PythonExecOutputFileTest" --no-daemon</automated>
  </verify>
  <done>EXEC-05 verified: unit tests pass and DSL documents outputFile</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PythonExec → project filesystem | Task writes user-configured outputFile path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | outputFile path traversal | mitigate | Gradle RegularFileProperty resolves under project; document relative paths in DSL |
| T-02-04 | Denial of Service | Large stdout write | accept | Same capture limits as existing stdout property |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs |
</threat_model>

<verification>
`./gradlew test check --no-daemon` passes including new outputFile tests and prior functional tests from 02-01.
</verification>

<success_criteria>
- EXEC-05: Setting `outputFile` persists captured stdout with parent dir creation
- Unit tests cover success, failure-before-throw, and nested paths
- DSL documents the new property
</success_criteria>

<output>
Create `.planning/phases/02-v1.1-features/02-02-SUMMARY.md` when done
</output>
